{% extends 'basefront.html.twig' %}

{% block title %}Guidesentretiens{% endblock %}

{% block bodysection %}
    <h1>Guidesentretiens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdGuide</th>
                <td>{{ guidesentretien.idGuide }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ guidesentretien.domaine }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ guidesentretien.specialite }}</td>
            </tr>
            <tr>
                <th>Support</th>
                <td>{{ guidesentretien.support }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ guidesentretien.note }}</td>
            </tr>
            <tr>
                <th>Nombrenotes</th>
                <td>{{ guidesentretien.nombrenotes }}</td>
            </tr>
        </tbody>
    </table>

    <div style="display: flex; justify-content: space-between; margin-top: 1em;">
        <a href="{{ path('app_guidesentretiens_index') }}" class="btn btn-primary">Back to list</a>

        <div>
            <a href="{{ path('app_guidesentretiens_edit', {'idguide': guidesentretien.idguide}) }}" class="btn btn-success">Edit</a>

            {{ include('guidesentretiens/_delete_form.html.twig') }}
        </div>
    </div>

    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            text-align: left;
            padding: 8px;
        }
        tr:nth-child(even){background-color: #f2f2f2}
        th {
            background-color: #4CAF50;
            color: black;
        }
        .btn {
            display: inline-block;
            margin-bottom: 0;
            font-weight: 400;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            -ms-touch-action: manipulation;
            touch-action: manipulation;
            cursor: pointer;
            background-image: none;
            border: 1px solid transparent;
            padding: 6px 12px;
            font-size: 14px;
            line-height: 1.42857143;
            border-radius: 4px;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }
        .btn-primary {
            color: #fff;
            background-color: #337ab7;
            border-color: #2e6da4;
        }
        .btn-primary:hover,
        .btn-primary:focus,
        .btn-primary:active,
        .btn-primary.active,
        .open > .dropdown-toggle.btn {
            /* CSS rules for the primary button */
        }
    </style>
{% endblock %}
