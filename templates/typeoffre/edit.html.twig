{% extends 'baseback.html.twig' %}
{% block title %}Modifier un type d'offre{% endblock %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header text-center">
                        <h4>Modifier un type d'offre</h4>
                    </div>
                    <div class="card-body">
                        {{ include('typeoffre/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    <div class="card-footer text-right">
                        <a class="btn btn-success btn-sm" href="{{ path('app_typeoffre_index') }}">Retour à la liste</a>
                        {{ include('typeoffre/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}






