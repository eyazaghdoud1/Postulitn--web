{% extends 'baseback.html.twig' %}
{% block title %} Edit Typeoffre {% endblock %}

{% block content %}
    

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header text-center">
                        <h4>Modifier un type d'offre</h4>
                    </div>
                    <div class="card-body">
                        {{ include('typeoffre/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    <div class="card-footer text-right">
                        <button class="btn btn-primary btn-sm" onclick="window.location='{{ path('app_typeoffre_index') }}'">Retour à la liste</button>
                        {{ include('typeoffre/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

