{% extends 'baseback.html.twig' %}

{% block title %}Typeoffre - voir offre{% endblock %}

{% block content %}

    <h1 style="text-align:center;">Typeoffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idtype</th>
                <td>{{ typeoffre.idtype }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typeoffre.description }}</td>
            </tr>
        </tbody>
    </table>

    <div style="text-align:center;">
        <a href="{{ path('app_typeoffre_index') }}">back to list</a>

        <a href="{{ path('app_typeoffre_edit', {'idtype': typeoffre.idtype}) }}">edit</a>

        {{ include('typeoffre/_delete_form.html.twig') }}
    </div>

{% endblock %}
