{% extends 'baseback.html.twig' %}

{% block title %}Ajouter un type d'offre{% endblock %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header text-center">
                        <h4 class="mb-0">Ajouter un type d'offre</h4>
                    </div>
                    <div class="card-body">
                        {{ include('typeoffre/_form.html.twig') }}
                    </div>
                    <div class="card-footer">
                        <a class="btn btn-secondary" href="{{ path('app_typeoffre_index') }}">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
