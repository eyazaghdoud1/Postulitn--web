{% extends 'baseback.html.twig' %}

{% block title %} ajouter-type {% endblock %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header text-center">
                        <h4>Ajouter un type d'offre</h4>
                    </div>
                    <div class="card-body">
                        {{ include('typeoffre/_form.html.twig') }}
                    </div>
                    <div class="card-footer text-right">
                        <a class="btn btn-primary btn-sm" href="{{ path('app_typeoffre_index') }}">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}








