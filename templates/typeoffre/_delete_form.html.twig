<form method="post" action="{{ path('app_typeoffre_delete', {'idtype': typeoffre.idtype}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeoffre.idtype) }}">
    <button class="btn btn-danger btn-sm btn-danger btn-sm">Delete</button>
</form>
